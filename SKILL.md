---
name: obsidian-note-writer
description: 'Obsidian 个人知识库管理系统（Knowledge Management Agent）。不止生成 Markdown 笔记，更负责知识库的长期治理：知识分类引擎（领域/一级/二级分类/层级/路径）、统一 Vault 目录规划、创建前重复检测（更新/子笔记/关联笔记三选一）、MOC 与索引自动维护、前置/相关/后续关系管理与双向链接、知识生命周期管理（learning/mastered/review/archived）、事实验证分级。When to use: 用户要求"写笔记/整理知识点/建MOC/分类归档/规划学习路线/出面试题"，或提供代码与文档要求整理进 Obsidian 知识库时触发。纯方法论，不绑定任何学科，全行业通用。'
agent_created: true
---

# Obsidian Knowledge Management Agent（个人知识库管理系统）

## 定位：你不是 Writer，是 Agent

本 skill 承担知识库的 **六大治理职责**，全部是默认行为：

| # | 职责 | 说明 |
|---|------|------|
| 1 | **创建知识** | 按等级（L1/L2/L3）生成高质量笔记 |
| 2 | **分类知识** | 分类引擎：领域 → 一级 → 二级 → 层级 → 存储路径 |
| 3 | **归档知识** | 归入统一 Vault 目录，禁止随意新建分类 |
| 4 | **维护索引** | MOC 与 `.vault-meta/` 状态文件自动同步 |
| 5 | **管理关系** | 前置/相关/后续 + 双向链接 |
| 6 | **支撑长期学习** | 生命周期状态 + 间隔复习 + 面试模式 |

## 核心流程（Agent 循环，每次任务完整走一遍）

```
读取 Vault（目录 + .vault-meta 索引）
   ↓
分析结构（分类引擎：领域/分类/层级/路径）
   ↓
定位已有知识（重复检测：更新 / 子笔记 / 关联笔记）
   ↓
生成笔记（按等级匹配模板与要素）
   ↓
更新索引（MOC + .vault-meta + 双向链接）
   ↓
保存（写入文件，返回变更摘要）
```

**必须使用文件系统工具执行**（Bash / Glob / Grep / Read / Write / Edit），而不是"建议用户去做什么"：查目录、搜已有笔记、改 MOC、写索引，都是 skill 的分内动作。

## 一、笔记等级（内容深度的第一决策点）

| 等级 | 适用对象 | 内容量 | 必备要素 |
|------|---------|--------|---------|
| **L1 速记** | 单个 API、单个概念 | ≤ 1000 字 | 定义 + 核心机制 + 一个例子 |
| **L2 知识点** | 一个主题 | 1000~2500 字 | L1 + 为什么 + 对比表 + 坑 + 自测 |
| **L3 专题** | 一个体系 | 2500 字以上 | L2 + MOC + 关系图（小 mermaid 或表格）+ 学习路线 + 多篇关联 |

选级规则：一个方法 → L1；一个概念 → L2；一组有演进关系的概念 → L3。**拿不准就低不就高**。

## 二、Agent 流程分步导航

| 阶段 | 做什么 | 文件 |
|------|--------|------|
| **1. 读取与分析** | 架构感知（读目录 + 模式识别），自适应归档（复用/优化/新建三选一） | `workflow/analyze.md`、`workflow/classify.md` |
| **2. 冲突检测** | 四层检测 + 粒度判断（A 合并/B 子笔记/C 独立/D 关联） | `workflow/analyze.md`（Step 4）、`features/conflict-detection.md` |
| **3. 写作** | 按等级匹配要素，事实分级标注 | `workflow/write.md`、`features/fact-check.md` |
| **4. 自检与复习** | 质量门禁 + 复习字段 | `workflow/review.md`、`references/learning-system.md` |
| **5. 维护** | 按权限矩阵执行索引更新、MOC 同步、生命周期流转 | `workflow/maintain.md`、`features/modification-rules.md`、`references/vault-meta.md` |
| **6. 关系维护** | 前置/相关/后续 + 双向补链 | `features/knowledge-map.md` |

## 三、模板导航

| 模板 | 等级 | 文件 |
|------|------|------|
| 速记模板 | L1 | `templates/quick-note.md` |
| 知识点模板 | L2 | `templates/knowledge-note.md` |
| 专题模板 | L3 | `templates/deep-dive.md` |

写作时复制对应模板，替换 `{{占位符}}`。

## 四、规范参考

| 文件 | 内容 |
|------|------|
| `references/obsidian.md` | Obsidian 使用规范：三层管理（目录+元数据+关系）、链接/MOC/知识点规范（含链接跟随设置） |
| `references/vault-meta.md` | `.vault-meta/` 可选状态索引：index.json / taxonomy.yaml / relations.json / changelog.md |
| `references/coding-note.md` | 代码类笔记五要素 + 环境版本信息 |
| `references/learning-system.md` | 知识生命周期（learning/mastered/review/archived）+ 间隔复习 |

## 五、特色能力（features/）

| 能力 | 触发时机 | 文件 |
|------|---------|------|
| **知识关系维护** | 每篇 L2/L3 写完后 | `features/knowledge-map.md` |
| **学习路线生成** | "我要学 XX / 规划路径" | `features/learning-roadmap.md` |
| **事实验证** | 引用非用户素材的技术事实 | `features/fact-check.md` |
| **面试复习模式** | "出面试题 / 模拟面试 / 考前冲刺" | `features/interview-mode.md` |
| **架构锁定** | vault 分类变更 / 大规模迁移时 | `features/architecture.md` —— 架构状态（stable/experimental/migration）+ 迁移方案 |
| **增量整合** | 创建任何新笔记前 | `features/conflict-detection.md` —— 四层检测 + 关系分类 + 粒度判断 + 用户确认 |
| **修改权限** | 涉及 MOC/索引/移动/合并等 L2+L3 操作时 | `features/modification-rules.md` —— Level 0~3 四级权限 + 架构状态 × 操作白名单矩阵 |

## 六、Agent 快速开始（完整任务清单）

1. 读取 Vault 目录 + `.vault-meta/index.json`，了解现状。
2. 按 `workflow/classify.md` 做架构感知 → 三选一（复用/优化/新建）自适应归档。
3. 按 `workflow/analyze.md` 做重复检测：能扩展已有笔记就不新建。
4. 定等级（L1/L2/L3）→ 套模板 → 按 `workflow/write.md` 写作。
5. 跑 `workflow/review.md` 自检 → 补生命周期字段。
6. 执行 `workflow/maintain.md`：更新 MOC、`relations.json`、`changelog.md`，双向补链。
7. 返回**变更摘要**（新建/修改/归档了什么、索引更新了什么）。

## 七、写作风格红线（防表格/图滥用，重要）

本 skill 不禁止表格，但**严格区分"讲解型内容"与"对比型内容"**，禁止用表格替代表述原理。

### 7.1 内容分两类，写法不同

| 类型 | 含义 | 正确写法 |
|------|------|---------|
| **讲解型** | 逐层/逐模块的原理、职责、机制、协议详解（如"每一层负责什么""某协议怎么工作"） | **文字段落逐点说明**，每层/每协议独立成小节展开，不缩成表 |
| **对比型** | 两个及以上对象的横向比较（如 TCP vs UDP）、模型对照（OSI vs TCP/IP）、速查索引（协议→层） | 表格（维度清晰、适合横比） |

### 7.2 红线（违反即错）

- ❌ 把"逐层/逐模块原理详解"做成一张总览表（如一张表塞下 7 层职责+协议）。
- ❌ 用「项目 \| 内容」小表缩写本该文字展开的机制、协议、职责。
- ❌ 把 L3 的"可视化"误解为"全文表格化"——可视化仅指关系图（小 mermaid 或表格二选一），**核心机制必须文字**。
- ✅ 讲解型：文字逐点（职责 + 数据单元 + 关键协议逐条 + 要点）。
- ✅ 表格仅用于：纯对比（TCP vs UDP）、模型对照（OSI vs TCP/IP）、速查表（协议→所属层）、属性列举（可选）。
- ✅ 大 mermaid 图（>8 节点、多分支）不用，小图或表格替代；但"逐层详解"不要用表替文字。

### 7.3 判定口诀

**"讲原理用字，比东西用表"**——看到"负责什么 / 怎么工作 / 为什么"，写文字；看到"区别 / 对照 / 速查"，做表格。

## 八、UML / 关系图数量上限（防溢出，重要）

Obsidian 中节点/连线过多的 mermaid 关系图会**超出屏幕、连线交错**，阅读体验极差。生成任何 UML/mermaid 图前必须先清点节点与连线数量。

- **阈值（可配置，当前 = 节点 12 / 连线 18）**：单个关系图 **节点数 > 12 或 连线数 > 18** 时，**触发警告**，提示"图可能过密，建议拆分、转表格或用文字描述"，**不再是硬拒绝**；极端情况（节点 > 18 或连线 > 24）强烈建议拆分。详见「九、图的类型与适配方案」。
- **单节点标签长度限制（防横向撑爆，硬约束）**：节点 `[]` / `()` 内的标签**建议 ≤ 8 个汉字**（或 ≤ 12 个 ASCII 字符）。过长的标签（如 `流量控制 滑动窗口 rwnd`、`拥塞控制 慢启动/避免 cwnd`）会让节点框横向过宽，Obsidian 单栏渲染时右侧节点被挤出可视区、连线标签也被推到屏外。细节（rwnd/cwnd/协议名/算法名）必须下沉到正文，图中只保留**一级概念词**（如 `流量控制`、`拥塞控制`、`连接管理`）。
- 超过阈值时改用：
  - 「关系清单表格」（前置 / 相关 / 后续 + 关系类型），或
  - 文字段落逐条叙述，或
  - 拆成多个 ≤10 节点的小图分别展示。
- **计数规则**：统计图中 `[]` / `()` 等节点框数量与 `-->` / `-.->` 等连线数量；含 `subgraph` 时按展开后的真实节点与连线计。
- **标签长度硬约束（防横向撑爆）**：节点 `[]` / `()` 内标签 **≤ 8 汉字（或 ≤ 12 ASCII 字符）** 为**强制红线**——长标签（如 `流量控制 滑动窗口 rwnd`）会让节点框横向过宽、Obsidian 单栏右侧被挤出屏外。细节（rwnd/cwnd/协议名/算法名）必须下沉正文，图中只留一级概念词。此约束优先级高于「L3 可视化/关系图」要素，与用户跨项目偏好「>10 节点 mermaid 撑屏难看」一致。**注意：节点/连线数量上限已柔性为警告（不硬拦图），仅标签长度维持强制。**

## 九、图的类型与适配方案（防"乱画图"与"该画不画"，重要）

写笔记时"要不要画图、画成什么样"按**内容语义**分型，而不是按工具分型。一张图该不该画、用什么渲染，先看它要表达什么。

### 9.1 四类图（按语义）

| 类型 | 表达什么 | 典型场景 | 渲染方式 | 约束 |
|------|----------|----------|----------|------|
| **A 关系/位置图** | 实体间层级、包含、依赖、承接 | 协议栈位置、模块依赖、笔记承接关系 | mermaid `graph` | 建议节点 ≤ 12、连线 ≤ 18；超阈值→主动提示"图可能过密，考虑拆分/转表/文字"（**指导性，非硬拒**）；单节点标签 ≤ 8 字**维持强制**（防横向撑爆，见 9.4） |
| **B 结构/封装图** | 内部组成、字段位域布局、谁套谁 | TCP 首部位域、IP 包嵌套 TCP 段、报文封装 | **ASCII 文本框图**（`text` 代码块） | 宽度 ≤ 75 字符；等宽字体对齐；框线风格一篇内统一（默认 `┌─┐`，不支持时降级 `+--+`） |
| **C 流程/时序/状态图** | 随时间的交互、状态变迁 | 三次握手、四次挥手、TCP 状态机 | mermaid `sequenceDiagram` / `stateDiagram-v2` 或 ASCII 双向箭头 | 时序图 `->>` 请求/调用、`-->>` 响应/返回；状态图 `[*]` 起点/终点（遵 mermaid 官方语义） |
| **D 对比表** | A vs B、属性列举、速查 | TCP vs UDP、首部字段表 | markdown 表格 | 表优于图判定见 9.3 |

### 9.2 选型决策（落笔前问自己）

- 讲"谁和谁关联 / 谁在谁上面" → **A 关系图**
- 讲"一个东西内部长啥样 / 字段怎么排 / 谁封装谁" → **B 结构图（ASCII，不是 mermaid）**
- 讲"随时间的步骤 / 交互 / 状态变化" → **C 时序/状态图**
- 讲"对比两个 / 列属性 / 速查" → **D 对比表**
- 纯原理逐层展开、无结构/流程/对比 → **文字段落，不强行配图**

### 9.3 表优于图的判定（D 类细化）

- **3 行以内** → 直接用文字叙述，不必上表
- **4–10 行** → 用表格（维度清晰、适合横比）
- **10 行以上** → 考虑拆表，或转 B 类 ASCII 结构图

### 9.4 约束分层与纠错

- **图 ≠ 表**：B 结构图是"形态可视化"，D 表格是"清单"，二者不可互替——这是"该画结构图却被表掉"的根因。
- **ASCII 结构图（B 类）不受第八章 mermaid 节点/连线/标签上限管辖**：它不是节点图，但有独立的**宽度约束**（≤75 字符，单栏不撑爆）+ 可读性规范（等宽对齐、框线风格统一）。
- **不为"有图"而图**：纯文字原理不硬配图，避免回到"滥用图"老路。
- 与第八章衔接：第八章节点>12/连线>18 为**警告阈值**（提示过密/拆分），标签≤8字为**强制红线**；第九章 A 类以"建议≤12/≤18"为日常目标，二者数字一致、口径统一。
